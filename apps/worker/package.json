{
  "name": "@app/worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": { "dev": "tsx watch --env-file=../../.env src/main.ts", "start": "node --env-file=../../.env dist/main.js", "build": "tsup src/main.ts --format esm --sourcemap", "typecheck": "tsc -p tsconfig.json --noEmit" },
  "dependencies": {
    "@repo/database": "workspace:*",
    "@repo/facebook-automation": "workspace:*",
    "@repo/queue": "workspace:*",
    "@repo/shared": "workspace:*",
    "pino": "9.9.5"
  },
  "devDependencies": { "tsx": "4.20.5" }
}
